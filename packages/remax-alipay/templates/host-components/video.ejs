<template name="TPL_video">
  <video <% for (let i = 0; i< props.length; i++) { %>
    <%=props[i]%>="{{node.props['<%=props[i]%>']}}" <% } %>
  >
    <template is="TPL" data="{{root: node}}" />
  </video>
</template>