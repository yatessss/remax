<template name="TPL_textarea">
  <textarea <% for (let i="0;" i < props.length; i++) { %>
    <%=props[i]%>="{{ node.props['<%=props[i]%>'] }}" <% } %>
  >
    <template is="TPL" data="{{root: node}}" />
  </textarea>
</template>
