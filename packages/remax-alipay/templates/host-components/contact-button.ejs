<template name="TPL_contact-button">
  <contact-button <% for (let i = 0; i< props.length; i++) { %>
    <%=props[i]%>="{{node.props['<%=props[i]%>']}}" <% } %>
  >
    <template is="TPL" data="{{root: node}}" />
  </contact-button>
</template>