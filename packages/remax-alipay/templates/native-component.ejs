<template name="TPL_<%=id%>">
  <<%=id%> <% for(let i=0;i < props.length; i++) { %>
    <%=props[i]%>="{{item.props['<%=props[i]%>']}}"<% } %>
    ref="{{item.props['__ref']}}">
    <block a:for="{{item.children}}" key="{{item.id}}">
      <block a:if="{{item.props['slot']}}">
        <view slot="{{item.props['slot']}}" <% for(let i=0;i < view.props.length; i++) { %>
          <%=view.props[i]%>="{{item.props['<%=view.props[i]%>']}}" <% } %>
        >
          <template is="TPL" data="{{root: item}}" />
        </view>
      </block>
      <block a:else>
        <template a:if="{{item.props._eid > 0}}" is="{{'TPL_' + item.props._eid}}" data="{{ node: item }}" />
        <template a:else is="{{ 'TPL_' + item.type }}" data="{{ node: item }}" />
      </block>
    </block>
  </<%=id%>>
</template>